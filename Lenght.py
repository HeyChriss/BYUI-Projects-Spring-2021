s_length = float(input('What is the length of a side of the square? '))
area_square = s_length * s_length
print(f'The area of the square is: {area_square}')
rec_length = float(input('What is the length of a rectangle? '))
rec_width = float(input('What is the width of a rectangle? '))
area_rec = rec_length * rec_width
print(f'The area of the rectangle is: {area_rec}')
radius = float(input('What is the radius of the circle? '))
#Did the other streech activity here:
import math
area_c = float(math.pi) * radius * radius
print(f'The area of the circle is: {area_c}')


# THis is the strech activity:

length = float(input('What is the length of a side of the square? '))
area_square = length * length
print(f'The area of the square is: {area_square}')
import math
area_circle = float(math.pi) * length * length
print(f'The area of the circle is: {area_circle}')
volume_square = length * length * length
print(f'The squares voloume is: {volume_square}')
volume_sphere = float(4/3) * float(math.pi) * volume_square
print(f'The volume of the sphere is: {volume_sphere}')
